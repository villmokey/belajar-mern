import request from 'supertest'
import Express from 'express';
const app = Express();

describe("POST API", () => {
  it("Should success to get posts data", () => {
    // localhost:5000/posts
  })
})